import {FC} from 'react';
import {Box} from '@mui/material';

export const Tasks: FC = () => {
  return (
    <Box display="flex" gap={4} flexWrap="wrap">
      Здесь пока не понятно, какие задачи показывать
    </Box>
  );
};
